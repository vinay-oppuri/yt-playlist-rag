[project]
name = "fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "vinay-oppuri", email = "oppurivinay25@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
fastapi = "fastapi:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
